node_modules/
.venv/
__pycache__/
*.pyc
*.pyo
.env
# Actual secrets on the server — .env.example is safe to commit
backend/.env
dist/
build/
.DS_Store
.idea/
.vscode/

# Local model artifacts (keep out of git)
backend/models/
backend/models/*
!backend/models/**/.gitkeep
backend/models/**/*.safetensors
backend/models/**/*.bin

# Local SQLite DB (recruiter auth / sessions)
backend/db.sqlite3
backend/*.sqlite3

# Local scratch/test files
tmp_test/

# ML training artifacts
*.log
runs/
wandb/
lightning_logs/

# Local dev backend — identical copy of insightats-backend/ (the real deployed HF Space repo)
# Kept locally for dev/testing only. Not tracked here to avoid code drift confusion.
backend/

# Separate HuggingFace git repos — have their own .git, deployed independently
# insightats-backend/ → https://huggingface.co/spaces/Suvradip01/insightats-backend
# insightats-models/  → https://huggingface.co/Suvradip01/insightats-models
insightats-backend/
insightats-models/

# Scratch / training artefacts at root
Readmemodel.txt
